{
  "name": "git-drive",
  "version": "0.1.0",
  "description": "Use an external drive as a git bare repository remote",
  "bin": {
    "git-drive": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
